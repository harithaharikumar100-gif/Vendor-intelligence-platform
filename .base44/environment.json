{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GROQ_API_KEY",
      "description": "Groq API key for LLM inference — get it from console.groq.com/keys",
      "requiredAtBoot": false
    },
    {
      "name": "SERPER_API_KEY",
      "description": "Serper API key for web search — get it from serper.dev",
      "requiredAtBoot": false
    }
  ]
}
